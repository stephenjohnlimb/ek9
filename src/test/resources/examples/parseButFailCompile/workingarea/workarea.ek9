#!ek9
<?-
  Examples of multiple 'calls' to a range of constructs to test the
  identifierReference instantiation with generics, classes components, records, etc.

  In this case just checking records can be created.
-?>
defines module bad.recordcalls.examples1

  //Some examples of creating a new 'constrained' type or just a sort of alias.
  defines record

    R1
      default private R1()

      R1()
        -> arg0 as String
        this()
        assert arg0?

  defines function

    TestPublicR1Construction()
      r1 <- R1("Steve")
      assert r1?

    TestPrivateR1Construction()

      @Error: FULL_RESOLUTION: NOT_ACCESSIBLE
      error1 <- R1()
      assert error1?

    TestNoSuchR1Constructor()
      @Error: FULL_RESOLUTION: METHOD_NOT_RESOLVED
      error2 <- R1(false)

      @Error: FULL_RESOLUTION: TYPE_NOT_RESOLVED
      assert error2?

//EOF