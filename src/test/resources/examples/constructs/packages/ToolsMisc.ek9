#!ek9
//For use with TCP example for resolving dependencies
defines module ekopen.org.net.tools.misc

  //For time being these need to be forward declared
  defines type
    List of String
    Dict of (String, String)

  defines package

    publicAccess <- true
    version <- 3.2.3-21
    description <- "Example Misc tools"
    license <- "MIT"

    tags <- [
      "general",
      "tools"
      ]

    //Just this source file and these files.
    includeFiles <- [
      "**.{cal}"
      ]

//EOF