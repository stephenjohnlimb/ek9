#!ek9
defines module net.circular.two

  defines package

    publicAccess as Boolean := true
    version as Version: 1.0.1-0
    description as String = "Simulation of circular references"
    license <- "MIT"

    deps <- {
      "net.circular.one": "1.0.0-0"
      }

//EOF