#!ek9
defines module fuzztest.operators.test023

  defines class

    Key
      id <- 0

      // Should fail - #? operator takes no parameters
      @Error: EXPLICIT_TYPE_SYMBOL_DEFINITION: TOO_MANY_ARGUMENTS
      operator #? as pure
        -> seed as Integer
        <- hash as Integer: id + seed

//EOF
