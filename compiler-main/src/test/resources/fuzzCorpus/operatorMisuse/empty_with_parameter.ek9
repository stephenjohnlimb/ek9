#!ek9
defines module fuzztest.operators.test025

  defines class

    Container
      items as List of Integer: List()

      // Should fail - ~ operator takes no parameters
      @Error: EXPLICIT_TYPE_SYMBOL_DEFINITION: TOO_MANY_ARGUMENTS
      operator ~ as pure
        -> deep as Boolean
        <- result as Boolean: items.empty()

//EOF
