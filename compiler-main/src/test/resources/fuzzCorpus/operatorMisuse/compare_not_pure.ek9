#!ek9
defines module fuzztest.operators.test004

  defines class

    Version
      major <- 1
      minor <- 0

      // Should fail - <=> operator not marked pure
      @Error: EXPLICIT_TYPE_SYMBOL_DEFINITION: OPERATOR_MUST_BE_PURE
      operator <=>
        -> other as Version
        <- result as Integer: Integer()
        if major == other.major
          result: minor <=> other.minor
        else
          result: major <=> other.major

//EOF
