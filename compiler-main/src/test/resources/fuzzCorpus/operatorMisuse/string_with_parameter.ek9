#!ek9
defines module fuzztest.operators.test024

  // @ExpectPhase: SYMBOL_DEFINITION
  // @ExpectError: OPERATOR_DOES_NOT_SUPPORT_PARAMETERS
  // @TestDescription: String operator does not take parameters

  defines class

    Data
      value <- 42

      // Should fail - $ operator takes no parameters
      @Error: EXPLICIT_TYPE_SYMBOL_DEFINITION: TOO_MANY_ARGUMENTS
      operator $ as pure
        -> format as String
        <- result as String: `${value}`

//EOF
