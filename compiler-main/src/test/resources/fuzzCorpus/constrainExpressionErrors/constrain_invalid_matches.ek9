#!ek9
<?-
  Tests using 'matches' operator on a type that doesn't have it.

  Expected error: METHOD_NOT_RESOLVED
  Phase: FULL_RESOLUTION

  Integer does not have a 'matches' operator, so using it in a
  constrain expression should fail at resolution.
-?>
defines module fuzztest.constrain.invalid.matches

  defines type

    BadIntegerMatches as Integer constrain as
      @Error: FULL_RESOLUTION: METHOD_NOT_RESOLVED
      matches 5

//EOF
