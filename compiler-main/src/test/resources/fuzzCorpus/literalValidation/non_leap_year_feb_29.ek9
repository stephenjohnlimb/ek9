#!ek9
<?-
  Literal Validation Fuzz Test: Date - Non-Leap Year February 29
  Tests if EK9 catches Feb 29 in non-leap year at compile time.

  Expected: Should fail (2023 is not a leap year)
  Actual: Compiles successfully, becomes unset at runtime
-?>
defines module fuzz.literal.date.nonleap

  defines program

    TestNonLeapYearFeb29()
      invalidDate <- 2023-02-29

      stdout <- Stdout()
      if invalidDate?
        stdout.println("Date is set")

//EOF
