#!ek9
<?-
  Literal Validation Fuzz Test: Version - All Zero Components
  Tests if EK9 accepts version with all zero components (0.0.0-0) at compile time.

  Expected: Should succeed (all zeros is technically valid semver)
  Actual: Compiles successfully, value behavior depends on runtime validation
-?>
defines module fuzz.literal.version.zero
  defines program
    Test()
      v <- 0.0.0-0
      stdout <- Stdout()
      if v?
        stdout.println("Set")
//EOF
