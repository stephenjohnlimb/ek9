#!ek9
<?-
  Literal Validation Fuzz Test: RegEx - Invalid Pattern Syntax
  Tests if EK9 catches regex with invalid group syntax at compile time.

  Expected: Should fail (regex pattern /(? / has incomplete group syntax)
  Actual: Compiles successfully, becomes unset at runtime
-?>
defines module fuzz.literal.regex.invalid
  defines program
    TestInvalidPattern()
      r <- /(?/
      stdout <- Stdout()
      if r?
        stdout.println("Set")
//EOF
