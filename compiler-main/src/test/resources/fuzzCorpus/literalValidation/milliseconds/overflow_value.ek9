#!ek9
<?-
  Literal Validation Fuzz Test: Millisecond - Overflow Value
  Tests if EK9 catches extreme millisecond value at compile time.

  Expected: Should fail or warn (value may cause overflow in millisecond representation)
  Actual: Compiles successfully, becomes unset at runtime
-?>
defines module fuzz.literal.millisecond.overflow
  defines program
    Test()
      ms <- 999999999999ms
      stdout <- Stdout()
      if ms?
        stdout.println("Set")
//EOF
