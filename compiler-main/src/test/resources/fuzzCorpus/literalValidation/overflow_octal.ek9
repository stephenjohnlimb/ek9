#!ek9
<?-
  Literal Validation Fuzz Test: Integer - Octal Overflow
  Tests if EK9 catches integer overflow in octal literal at compile time.

  Expected: Should fail (07777777777777777777777 exceeds maximum integer representation)
  Actual: Compiles successfully, becomes unset at runtime
-?>
defines module fuzz.literal.integer.octal.overflow
  defines program
    TestOverflowOctal()
      i <- 07777777777777777777777
      stdout <- Stdout()
      if i?
        stdout.println("Set")
//EOF
