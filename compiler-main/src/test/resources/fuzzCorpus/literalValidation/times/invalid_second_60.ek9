#!ek9
<?-
  Literal Validation Fuzz Test: Time - Second 60
  Tests if EK9 catches second = 60 at compile time.

  Expected: Should fail (second must be 0-59)
  Actual: Compiles successfully, becomes unset at runtime
-?>
defines module fuzz.literal.time.second60

  defines program

    TestSecond60()
      invalidTime <- 12:30:60

      stdout <- Stdout()
      if invalidTime?
        stdout.println("Time is set")

//EOF
