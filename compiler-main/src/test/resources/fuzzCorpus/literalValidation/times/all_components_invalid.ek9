#!ek9
<?-
  Literal Validation Fuzz Test: Time - All Components Out of Range
  Tests if EK9 catches all invalid time components at compile time.

  Expected: Should fail (99:99:99 is completely invalid)
  Actual: Compiles successfully, becomes unset at runtime
-?>
defines module fuzz.literal.time.all99

  defines program

    TestAllInvalid()
      invalidTime <- 99:99:99

      stdout <- Stdout()
      if invalidTime?
        stdout.println("Time is set")

//EOF
