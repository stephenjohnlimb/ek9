#!ek9
<?-
  Runtime Fuzz Test: For-range loop with 'by' in wrong direction

  Tests that for-range loops with 'by' clause in wrong direction
  (positive range with negative increment) correctly produces no output.

  Expected: No iterations because 1 ... 10 by -1 is impossible
-?>
defines module fuzz.runtime.forrange.wrongdirectionby

  defines program

    ForRangeWrongDirectionBy()
      stdout <- Stdout()

      stdout.println("before")

      for i in 1 ... 10 by -1
        stdout.println($i)

      stdout.println("after")

//EOF
