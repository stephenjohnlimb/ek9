#!ek9
<?-
  Runtime Fuzz Test: For-range loop with explicit 'by' increment value

  Tests that for-range loops with explicit 'by' clause correctly
  use the specified increment value (using += mutating operator).

  Expected: Iterate with step of 2: 1, 3, 5, 7, 9
-?>
defines module fuzz.runtime.forrange.byvalue

  defines program

    ForRangeWithByValue()
      stdout <- Stdout()

      for i in 1 ... 10 by 2
        stdout.println($i)

//EOF
