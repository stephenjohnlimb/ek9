#!ek9
<?-
  Runtime Fuzz Test: For-range single iteration verification

  Verifies that a range of 1 ... 3 iterates exactly 3 times
  with values 1, 2, 3 in order.

  This is a baseline sanity check for ascending iteration count.
-?>
defines module fuzz.runtime.forrange.singleiteration

  defines program

    ForRangeSingleIteration()
      stdout <- Stdout()

      // Simple ascending range: should iterate exactly 3 times
      for i in 1 ... 3
        stdout.println($i)

      stdout.println("done")

//EOF
