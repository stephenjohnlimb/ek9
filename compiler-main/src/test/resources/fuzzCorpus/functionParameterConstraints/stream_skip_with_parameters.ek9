#!ek9
<?-
  FUNCTION_MUST_HAVE_NO_PARAMETERS: Function with parameters used in stream skip.
  Tests that skip operation requires zero-parameter functions.
-?>
defines module fuzztest.functionparam.skip

  defines function
    AcceptsParameter()
      -> limit as Integer
      <- rtn as Integer: limit

    invalidStreamSkipWithParameterFunction()
      collection <- List() of String
      @Error: FULL_RESOLUTION: FUNCTION_MUST_HAVE_NO_PARAMETERS
      cat ["first", "second", "third", "fourth"] | skip AcceptsParameter > collection
      assert collection?

//EOF
