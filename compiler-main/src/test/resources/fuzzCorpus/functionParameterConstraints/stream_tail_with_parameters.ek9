#!ek9
<?-
  FUNCTION_MUST_HAVE_NO_PARAMETERS: Function with parameters used in stream tail.
  Tests that tail operation requires zero-parameter functions.
-?>
defines module fuzztest.functionparam.tail

  defines function
    AcceptsParameter()
      -> count as Integer
      <- rtn as Integer: count

    invalidStreamTailWithParameterFunction()
      collection <- List() of String
      @Error: FULL_RESOLUTION: FUNCTION_MUST_HAVE_NO_PARAMETERS
      cat ["first", "second", "third"] | tail AcceptsParameter > collection
      assert collection?

//EOF
