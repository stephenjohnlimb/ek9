#!ek9
defines module fuzztest.experiment.test002

  // @ExpectPhase: PARSING
  // @ExpectError: SYNTAX_ERROR (service declaration missing URI)
  // @TestDescription: Service with identifier but no URI

  defines service
    MyService

      getUser() as GET for /api/users/{id}
        <- rtn as String: "user"

//EOF
