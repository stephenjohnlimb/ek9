#!ek9
<?-
  A couple of very simple programs.
  With command line arguments, one String and one An Integer.
  We will add others later.
-?>
defines module introduction

  defines program

    PassThrough()
      stdin <- Stdin()
      stdout <- Stdout()
      stdout.println("Enter Text:")
      inputLine <- stdin.next()
      stdout.println(inputLine)
//EOF