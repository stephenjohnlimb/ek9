#!ek9
defines module introduction

  defines program

    HelloMessage()
      -> message as String

      regex <- /[a-zA-Z0-9]{6}/
      assert regex?
      stdout <- Stdout()
      stdout.println(message)

//EOF