#!ek9
defines module operatorUse


  defines function

    equality()
      -> i1 as Integer
      assert i1?

<!-
    aSimpleFunction()
      -> arg0 as String
      <- rtn as String: String()

      someLocal as String?
      someLocal = "Hi"
      assert someLocal?

      set <- arg0?
      assert set

      claude <- "Hello Claude"
      rtn: claude

  defines program
    HelloWorld()

      //While Stdout is not defined here, it should be resolvable
      @Resolved: SYMBOL_DEFINITION: TYPE: "Stdout"
      stdout <- Stdout()
      toOutput <- "Hello, World"
      stdout.println(toOutput)
-!>

//EOF