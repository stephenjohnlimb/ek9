#!ek9
<?-
  Test: Calling non-pure methods on constants should produce NOT_MUTABLE error.

  BUG: Currently V1.incrementBuildNumber() compiles without error,
  but it should fail because V1 is a constant and incrementBuildNumber()
  is a mutating (non-pure) method.
-?>
defines module test.constant.method.mutation

  defines constant
    V1 <- 1.0.0-0

  defines function

    testMutatorMethodOnConstant()
      @Error: FULL_RESOLUTION: NOT_MUTABLE
      V1.incrementBuildNumber()

//EOF