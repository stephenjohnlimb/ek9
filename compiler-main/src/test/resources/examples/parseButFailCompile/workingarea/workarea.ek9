#!ek9
<?-
  Test IR generation for simple switch with literal integer comparisons.
  Statement form only - no return value from switch.
-?>
defines module controlFlow

  defines function


    simpleSwitchLiteral()
      -> value as Integer
      <-
        result <- String()

      switch value
        case 1
          result := "One"
        case 2
          result := "Two"
        default
          result := "Other"

//EOF
