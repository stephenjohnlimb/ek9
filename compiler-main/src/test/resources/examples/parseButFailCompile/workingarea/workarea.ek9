#!ek9
defines module bytecode.test

  defines program

    SimpleNullCoalesce()

      stdout <- Stdout()

      // Test 1: Dict.get() returns null
      dict <- Dict() of (String, Integer)
      x <- dict.get("missing")  // Returns null
      y <- 10
      result <- x ?? y
      stdout.println(result)  // Should print 10

//EOF
