#!ek9
defines module net.customer
    
  defines package
    publicAccess as Boolean := true
    version <- 1.0.0-0
    description as String = "Simulation of a package"
    license <- "MIT"

    //Two different ways to declare Lists    
    tags <- [ "tools" ]
    
    includeFiles <- [
      "**.{csv,jpeg}"
      ]

//EOF